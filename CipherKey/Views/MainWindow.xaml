<ui:FluentWindow x:Class="CipherKey.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CipherKey"
        mc:Ignorable="d"
        xmlns:vm="clr-namespace:CipherKey.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
        WindowStartupLocation="CenterScreen"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="CipherKey" Height="600" Width="950">
    <ui:FluentWindow.Resources>
        <Storyboard x:Key="ExpandModuleView">
            <DoubleAnimation Storyboard.TargetName="ModuleView" Storyboard.TargetProperty="(ContentControl.Width)"
                             To="400" Duration="0:0:0.3"/>
        </Storyboard>
        <Storyboard x:Key="CollapseModuleView">
            <DoubleAnimation Storyboard.TargetName="ModuleView" Storyboard.TargetProperty="(ContentControl.Width)"
                     To="0" Duration="0:0:0.3"/>
        </Storyboard>
    </ui:FluentWindow.Resources>
    <Grid PreviewMouseLeftButtonDown="Grid_PreviewMouseLeftButtonDown">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ContentControl Width="0" Grid.RowSpan="100" Grid.ColumnSpan="100" Panel.ZIndex="999" Content="{Binding ModuleView, UpdateSourceTrigger=PropertyChanged}" x:Name="ModuleView" HorizontalAlignment="Right" Margin="0 25 0 25"/>
        
        <!-- DragMove -->
        <Grid PreviewMouseLeftButtonDown="Grid_PreviewMouseLeftButtonDown" Grid.ColumnSpan="100" Background="{ui:ThemeResource }" MouseDown="RowDefinition_MouseDown"></Grid>
        
        <!-- Header information -->
        <StackPanel PreviewMouseLeftButtonDown="Grid_PreviewMouseLeftButtonDown" Orientation="Horizontal" Grid.ColumnSpan="100" Margin="15" Grid.RowSpan="100" Panel.ZIndex="99">
            <TextBlock Text="CipherKey"/>
        </StackPanel>
        
        
        <!-- Right Window Commands -->
        <StackPanel PreviewMouseLeftButtonDown="Grid_PreviewMouseLeftButtonDown" Panel.ZIndex="99" Orientation="Horizontal" Grid.ColumnSpan="100" Grid.RowSpan="100" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="15">
            <Button BorderThickness="0" Grid.ColumnSpan="100" Grid.RowSpan="100" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="5 0 0 0" Command="{Binding CloseCommand}">
                <ui:SymbolIcon Symbol="ArrowMinimize16" Foreground="Orange" Height="20" Width="20" FontSize="20"/>
            </Button>
            <Button BorderThickness="0" Grid.ColumnSpan="100" Grid.RowSpan="100" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="5 0 0 0" Command="{Binding CloseCommand}">
                <ui:SymbolIcon Symbol="ArrowExit20" Foreground="Red" Height="20" Width="20" FontSize="20"/>
            </Button>
        </StackPanel>

        <ScrollViewer Grid.Row="1" PreviewMouseLeftButtonDown="Grid_PreviewMouseLeftButtonDown">
            <StackPanel Orientation="Vertical" Margin="2 15 0 2">
                <ListView ItemsSource="{Binding Topics, UpdateSourceTrigger=PropertyChanged}">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical"></StackPanel>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0 5 15 0">
                                <Border CornerRadius="100" Height="35" Width="35" BorderThickness="2" BorderBrush="{Binding Design.ForegroundHex}">
                                    <Border.Background>
                                        <ImageBrush ImageSource="{Binding Design.TopicImage, UpdateSourceTrigger=PropertyChanged}" Stretch="UniformToFill" />
                                    </Border.Background>
                                </Border>
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Margin="15 0 0 0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Button HorizontalAlignment="Stretch" Height="35" Command="{Binding CreateTopicCommand}">
                    <ui:SymbolIcon Symbol="Add12"/>
                </Button>
            </StackPanel>  
        </ScrollViewer>
        
        
    </Grid>
</ui:FluentWindow>
